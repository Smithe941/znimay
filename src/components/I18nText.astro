---
interface Props {
  uk: string;
  en: string;
  class?: string;
}

const { uk, en, class: className = '' } = Astro.props;
---

<span class:list={['i18n-uk', className]}>{uk}</span><span class:list={['i18n-en', className]}>{en}</span>
